# Move robot end-effector linearly to target pose
geometry_msgs/Pose target_pose
float64 speed  # Speed factor (0.0 - 1.0)
float64 acceleration  # Acceleration factor (0.0 - 1.0)
bool async_mode  # If true, return immediately without waiting for completion
---
bool success
string message
